{
  "name": "get-dev-paths",
  "version": "0.2.0",
  "main": "lib/index.js",
  "license": "MIT",
  "repository": "aleclarson/get-dev-paths",
  "description": "Search node_modules for locally developed packages",
  "dependencies": {
    "realpath-native": "^1.0.2",
    "type-error": "^1.0.3"
  },
  "devDependencies": {
    "coffeescript": "^2.3.0"
  },
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "coffee -o lib -c src",
    "dev": "coffee -w -o lib -c src"
  }
}
